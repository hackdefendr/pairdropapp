# PairDrop for iOS

Not started. It will import `shared/PairDropKit` unchanged — the protocol layer already
builds for iOS 17+ — so what lands here is the UI and the share extension.

Next after the macOS client has been used in anger for a while.
